
name: new test greet

on:
    push:
        branches:
            - main
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: THis will greet the user
              run : echo "hello pankaj"
            
